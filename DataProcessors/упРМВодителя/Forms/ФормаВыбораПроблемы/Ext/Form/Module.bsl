#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЗакрытьТекущуюФорму	= Ложь;
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	Отказ = НЕ ЗакрытьТекущуюФорму;	
КонецПроцедуры
	
#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура КнопкаВыбратьНажатие(Элемент)
	ВыбраноЗначение();
КонецПроцедуры

&НаКлиенте
Процедура КнопкаВыходНажатие(Элемент)
	сткВозврата	= Новый Структура;
	сткВозврата.Вставить("ИмяФормыПерехода", "ФормаИсполнениеТочек");
	ЗакрытьТекущуюФорму	= Истина;
	Закрыть(сткВозврата);
КонецПроцедуры

&НаКлиенте
Процедура СписокПроблемыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	ВыбраноЗначение();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыФункции

&НаКлиенте
Процедура ВыбраноЗначение()
	ТекущиеДанные = Элементы.СписокПроблемы.ТекущиеДанные;
	Если НЕ ТекущиеДанные = Неопределено Тогда
		ОповеститьОВыборе(ТекущиеДанные.Ссылка)
	Иначе
		ОповеститьОВыборе(ПредопределенноеЗначение("Справочник.упПроблемы.ПустаяСсылка"));
	КонецЕсли;
	сткВозврата =Новый Структура;
	сткВозврата.Вставить("ИмяФормыПерехода", "ФормаИсполнениеТочек");
	ЗакрытьТекущуюФорму = Истина;
	Закрыть(сткВозврата);
КонецПроцедуры
	
#КонецОбласти
