

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	Если Отказ Тогда
		Возврат;	
	КонецЕсли;
	
	МинимальноеЗначение = -1000;
	
	Для каждого ТекущаяСтрока Из Интервалы Цикл
	
		Если НЕ ТекущаяСтрока.Значение > МинимальноеЗначение Тогда
			ТекстСообщения = НСтр("ru = 'В табличной части Интервалы не верно заполнено значение в строке номер %1. Значение с каждой строкой должно быть больше предыдущего.'");
			ПредставлениеНомераСтроки = Формат(ТекущаяСтрока.НомерСтроки,"ЧН=0; ЧГ=0");
			ТекстСообщения = СтрШаблон(ТекстСообщения,ПредставлениеНомераСтроки);
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,Ссылка,СтрШаблон("ИнтервалыЗначение%1",ПредставлениеНомераСтроки),"Объект",Отказ);
			Прервать;
		КонецЕсли;
		
		МинимальноеЗначение = ТекущаяСтрока.Значение;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти 

#Область СлужебныеПроцедурыИФункции
// Нет.
#КонецОбласти 