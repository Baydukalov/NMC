
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ДополнительнаяОперация = Неопределено;
	Если Параметры.Отбор.Свойство("ДополнительнаяОперация", ДополнительнаяОперация) Тогда 
		Запрос = Новый Запрос;
		Запрос.Текст = "ВЫБРАТЬ
		|	упТипыДополнительногоОборудованияДополнительныеОперации.Ссылка
		|ИЗ
		|	Справочник.упТипыДополнительногоОборудования.ДополнительныеОперации КАК упТипыДополнительногоОборудованияДополнительныеОперации
		|ГДЕ
		|	упТипыДополнительногоОборудованияДополнительныеОперации.Операция = &Операция";
		Запрос.УстановитьПараметр("Операция", ДополнительнаяОперация);
		
		спзСсылок = Новый СписокЗначений;
		спзСсылок.ЗагрузитьЗначения(Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка"));
		
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список, "Ссылка", спзСсылок, ВидСравненияКомпоновкиДанных.ВСписке,, Истина);
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти