
#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	Показатели.Сортировать("ВходКон");
	
	ВходНач  = 0;
	ВыходНач = 0;
	Для Каждого текСтрока Из Показатели Цикл 
		текСтрока.ВходНач  = ВходНач;
		текСтрока.ВыходНач = ВыходНач;
		ВходНач  = текСтрока.ВходКон;
		ВыходНач = текСтрока.ВыходКон;
	КонецЦикла;	
	
КонецПроцедуры

#КонецОбласти 