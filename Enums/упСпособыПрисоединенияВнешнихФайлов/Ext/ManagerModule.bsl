
#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Адрес") Тогда
		СтандартнаяОбработка = Ложь;
		
		СписокСсылок = Новый СписокЗначений;
		СписокСсылок.Добавить(Перечисления.упСпособыПрисоединенияВнешнихФайлов.КПрохождениюТочки);
		СписокСсылок.Добавить(Перечисления.упСпособыПрисоединенияВнешнихФайлов.КРейсу);
		
		ДанныеВыбора = СписокСсылок;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти


