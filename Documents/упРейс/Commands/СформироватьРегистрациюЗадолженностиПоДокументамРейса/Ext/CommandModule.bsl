
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Отказ = Ложь;
	ТекстОшибки = "";
	ДокументСсылка = СформироватьРегистрацияЗадолженностиПоДокументамРейса(ПараметрКоманды, ТекстОшибки, Отказ);
	
	Если Не Отказ Тогда
		ОткрытьФорму("Документ.упРегистрацияЗадолженностиПоДокументамРейса.ФормаОбъекта", Новый Структура("Ключ", ДокументСсылка), ПараметрыВыполненияКоманды.Источник);
	Иначе
		Если ТекстОшибки = "" Тогда
			ТекстОшибки = НСтр("ru = 'Документ не сформирован.'");		    
		КонецЕсли; 
		//ПоказатьПредупреждение(, ТекстОшибки);
		упСервисныеФункцииКлиент.ПоказатьОповещениеПользователяНаше(ТекстОшибки, Истина);
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти 

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция СформироватьРегистрацияЗадолженностиПоДокументамРейса(Рейс, ТекстОшибки, Отказ)
	
	Возврат Документы.упРегистрацияЗадолженностиПоДокументамРейса.СформироватьРегистрацияЗадолженностиПоДокументамРейса(Рейс, ТекстОшибки, Отказ);
	
КонецФункции

#КонецОбласти 